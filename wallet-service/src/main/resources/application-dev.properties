# Active with SPRING_PROFILES_ACTIVE=dev (see docker-compose.dev.yml).
# DevTools restarts when the classpath changes; with a bind-mounted module, run a compile
# on the host (IDE build or ./mvnw compile) after editing sources so target/classes updates.
spring.devtools.restart.poll-interval=500ms
