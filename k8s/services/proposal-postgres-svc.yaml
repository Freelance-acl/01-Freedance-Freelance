apiVersion: v1
kind: Service
metadata:
  name: proposal-postgres-headless
  namespace: freelance
spec:
  clusterIP: None
  selector:
    app: proposal-postgres
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: proposal-postgres
  namespace: freelance
spec:
  type: ClusterIP
  selector:
    app: proposal-postgres
  ports:
    - port: 5432
      targetPort: 5432
