apiVersion: v1
kind: Service
metadata:
  name: user-postgres-headless
  namespace: freelance
spec:
  clusterIP: None
  selector:
    app: user-postgres
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: user-postgres
  namespace: freelance
spec:
  type: ClusterIP
  selector:
    app: user-postgres
  ports:
    - port: 5432
      targetPort: 5432
