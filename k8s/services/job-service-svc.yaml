apiVersion: v1
kind: Service
metadata:
  name: job-service
  namespace: freelance
spec:
  type: ClusterIP
  selector:
    app: job-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: metrics
      port: 8081
      targetPort: 8081
